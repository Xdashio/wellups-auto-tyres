# Release candidate v0.4.0-rc.1 — certification record

- Tag: `v0.4.0-rc.1` (annotated, immutable)
- Commit: `b8c435e` (merge of `release/v0.4` into `main`)
- Branch: `main` (production line since GATE 017)
- Migrations: 001–016 (015/016 certified live, untouched)
- Date: 2026-09-22
- Deployment status: **NOT DEPLOYED** (no deploy authorized or executed)

## What is certified

Production baseline: full application, 016 security hardening live,
real branch identity (Greenspan Mall, Donholm), intentionally empty
public catalog (SEED retired, history preserved), release controls.

## Evidence

| Check | Result |
|---|---|
| lint / typecheck / unit (46) | PASS on exact commit |
| `scripts/ci-db.sh` + pgTAP (016: 58) | PASS |
| `npm run build` (12 routes) | PASS |
| OpenNext artifact (`worker.js` + assets) | PASS (locally reproduced) |
| Live `admin_write_paths_live` | 11/11 |
| Live `quotes_live` | 10/10 |
| Booking evidence (accepted Path B) | PRIOR LIVE BOOKING BEHAVIOR VERIFIED; CURRENT LIVE LIFECYCLE RE-EXECUTION BLOCKED BY INTENTIONALLY EMPTY CATALOG; CODE PATH VERIFIED UNCHANGED (012B 20/20, 013 E2E 3/3, zero diff since) |
| Quote lifecycle live | create → quoted → accepted verified on current line |
| Browser (Chromium screenshots) | home, empty catalogs, admin, settings, 404, mobile |
| Anon boundaries | 8/8 staff views privilege-denied |
| Secret scan | clean (no service-role/JWT/private-key material) |

## Tag CI

- Trigger: VERIFIED (run 35714405884 created for the tag push)
- Execution: PENDING — GitHub account locked (billing); all jobs refuse to
  start, identically for every recent branch/tag run. NOT a code failure.
  Every CI step was reproduced green locally on the exact tagged commit
  (see above). Re-observe once billing is restored; do not deploy before.

## Deployment authorization

Fail-closed (`release.yml` refuses all runs; no CI deploy path; deploy only
via manual `opennextjs-cloudflare deploy`, never executed). Technically
ready for an explicit deployment authorization step; business readiness
(blockers: first real catalog rows, staff provisioning, domain) is separate.
